/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
var __webpack_exports__ = {};
var selectHeader = document.querySelectorAll(".fillter-select__header");
var selectItem = document.querySelectorAll(".fillter-select__item");
selectHeader.forEach(function (item) {
  item.addEventListener("click", selectToggle);
});
selectItem.forEach(function (item) {
  item.addEventListener("click", selectChouse);
});
function selectToggle() {
  this.parentElement.classList.toggle("isActive");
}
function selectChouse() {
  var text = this.innerText;
  var fillterSelect = this.closest(".fillter-select");
  var currentText = fillterSelect.querySelector(".fillter-select__current");
  currentText.innerText = text;
  fillterSelect.classList.remove("isActive");
}

// mobile menu

var menuBtn = document.querySelector(".block-menu");
var menuList = document.querySelector(".nav__list");
function showMenu() {
  menuList.classList.toggle("active");
}
menuBtn.onclick = showMenu;
/******/ })()
;